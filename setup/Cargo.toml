[package]
name = "uran_installer"
version = "1.0.0"
edition = "2021"

[dependencies]
zip = "0.6"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["shellapi", "winuser", "shlobj", "knownfolders", "combaseapi", "objbase", "shobjidl_core", "objidl", "winerror", "wtypes"] }